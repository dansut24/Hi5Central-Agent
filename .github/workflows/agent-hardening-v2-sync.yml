name: Sync chat files mobile CAD checkpoint

on:
  push:
    branches:
      - agent-hardening-v2
    paths:
      - ".github/workflows/agent-hardening-v2-sync.yml"

permissions:
  contents: write

jobs:
  sync-source:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout hardening branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download and verify chat files mobile CAD snapshot
        shell: bash
        run: |
          set -euo pipefail
          curl -fL --retry 3 --retry-delay 2 \
            https://downloads.hi5central.com/bootstrap-agent-sync/chat-files-mobile-cad-0a03d82.tar.gz \
            -o /tmp/chat-files-mobile-cad-0a03d82.tar.gz
          echo "63b652a5e0cd23e2de663a60b2ad2d6f96efc5e66a0fd545aec59c5b6eb5f1ee  /tmp/chat-files-mobile-cad-0a03d82.tar.gz" | sha256sum -c -

      - name: Apply exact changed files
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p /tmp/chat-files-mobile-cad
          tar -xzf /tmp/chat-files-mobile-cad-0a03d82.tar.gz -C /tmp/chat-files-mobile-cad
          cp -a /tmp/chat-files-mobile-cad/. .
          git add \
            Agent/chatpass_agent/src/platform/windows/windows_service.cpp \
            Agent/chatpass_agent/src/ui/native_chat_window.cpp \
            Viewer/chatpass_viewer/src/app/viewer_app.cpp \
            Viewer/chatpass_viewer/web/file_browser.html \
            Viewer/chatpass_viewer/web/index.html \
            Viewer/chatpass_viewer/web/renderer.js
          git status --short

      - name: Commit synced checkpoint
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if git diff --cached --quiet; then
            echo "No source changes to sync."
            exit 0
          fi
          git commit -m "Sync chat files mobile CAD checkpoint"
          git push origin HEAD:agent-hardening-v2
